export enum StatusValue {
  /** 禁用 */
  FORBIDDEN = 0,
  /** 正常使用 */
  NORMAL = 1,
}
export enum AvailableRange {
  // 博客页面（PC端、小程序、APP）
  BlogPage = 0,
  // 后台管理系统
  BackgroundManagementSystem = 1,
  // ALL(博客页面和后台管理系统)
  ALL = 2,
}
